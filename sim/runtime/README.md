# Runtime

This directory is reserved for local manual simulator runs.

It is ignored by git.

Suggested contents during development:

- per-peer stores
- temporary keys
- session logs
- generated traces
