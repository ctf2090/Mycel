{
  "$schema": "./topology.schema.json",
  "topology_id": "signature-mismatch",
  "description": "One fault source and two readers exercise deterministic signature failure ordering",
  "fixture_set": "fixtures/object-sets/signature-mismatch",
  "execution_mode": "single-process",
  "peers": [
    {
      "node_id": "node:peer-fault",
      "role": "seed",
      "bootstrap_peers": [],
      "endpoint": "local:peer-fault"
    },
    {
      "node_id": "node:peer-reader-a",
      "role": "reader",
      "bootstrap_peers": ["node:peer-fault"],
      "endpoint": "local:peer-reader-a"
    },
    {
      "node_id": "node:peer-reader-b",
      "role": "reader",
      "bootstrap_peers": ["node:peer-fault"],
      "endpoint": "local:peer-reader-b"
    }
  ],
  "expected_outcomes": [
    "signature-verification-failure",
    "no-invalid-index-write"
  ],
  "notes": [
    "Use this topology to observe deterministic fault ordering for signature mismatch handling."
  ]
}
