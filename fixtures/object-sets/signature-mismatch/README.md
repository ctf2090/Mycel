# signature-mismatch

Purpose:

- verify that a peer rejects an object or wire envelope with an invalid signature

Expected outcomes:

- signature failure is reported clearly
- invalid data is not exposed as verified state
