{
  "$schema": "./topology.schema.json",
  "topology_id": "resync-idempotency",
  "description": "One seed peer and one reader peer that syncs twice; the second sync must be a no-op",
  "fixture_set": "fixtures/object-sets/resync-idempotency",
  "execution_mode": "single-process",
  "peers": [
    {
      "node_id": "node:peer-seed",
      "role": "seed",
      "bootstrap_peers": [],
      "endpoint": "local:peer-seed"
    },
    {
      "node_id": "node:peer-reader-a",
      "role": "reader",
      "bootstrap_peers": ["node:peer-seed"],
      "endpoint": "local:peer-reader-a"
    }
  ],
  "expected_outcomes": [
    "sync-success",
    "matching-object-sets",
    "matching-replay-results",
    "matching-accepted-heads",
    "resync-idempotent"
  ],
  "notes": [
    "The fixture metadata resync_check flag drives the second sync pass.",
    "This topology proves that re-sync when already current is a safe no-op."
  ]
}
